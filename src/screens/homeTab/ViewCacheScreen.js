import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';

const ViewCacheScreen = () => {
    // Initialisations ---------------------
    // State -------------------------------
    // Handlers ----------------------------
    // View --------------------------------
    return (
        <Text>Cache details</Text>
    );
};

const styles = StyleSheet.create({});

export default ViewCacheScreen;
