import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';

const LogCacheScreen = () => {
    // Initialisations ---------------------
    // State -------------------------------
    // Handlers ----------------------------
    // View --------------------------------
    return (
       <Text>Log Cache</Text>
    );
};

const styles = StyleSheet.create({});

export default LogCacheScreen;
