import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';

const CacheNavigationScreen = () => {
    // Initialisations ---------------------
    // State -------------------------------
    // Handlers ----------------------------
    // View --------------------------------
    return (
        <Text>Cache navigation screen</Text>
    );
};

const styles = StyleSheet.create({});

export default CacheNavigationScreen;
